language: php

dist: trusty

php:
  - 7.2

before_install:
  - yes '' | pecl install -f apcu
  - composer install

script:
  - bin/phpunit
#  - vendor/bin/infection --min-covered-msi=85 --show-mutations --threads=1
  - vendor/bin/phpstan analyse -c phpstan.neon.travis.dist --level=5
