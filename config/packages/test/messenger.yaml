framework:
    messenger:
        transports:
            # https://symfony.com/doc/current/messenger.html#transport-configuration
            cache-warmup: 'in-memory://'
            rostering-file-uploaded:
                dsn: 'in-memory://'
                serializer: messenger.transport.symfony_serializer
