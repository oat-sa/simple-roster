parameters:
    test1: '%env(REDIS_DOCTRINE_CACHE_HOST)%' #fixme
    test2: '%env(REDIS_DOCTRINE_CACHE_PORT)%' #fixme
doctrine:
    dbal:
        driver: 'pdo_sqlite'
        url: 'sqlite:///%kernel.project_dir%/var/test.db3'
    orm:
        metadata_cache_driver:
            type: array
        query_cache_driver:
            type: array
        result_cache_driver:
            type: array
